## ===

# ✅ Python MCQs – Correct Answers

---

## 🔹 Variables, Scope & Mutability (Q1–Q10)

**Q1 → B** — Output: `5`
(local `x` inside function does not affect global)

**Q2 → B** — Output: `6`
(`global x` modifies global variable)

**Q3 → C** — Output: `[1, 2, 3]`
(list is mutable, append affects original)

**Q4 → B** — Output: `[1, 2]`
(reassignment inside function doesn’t affect original)

**Q5 → A** — Output: `10`
(integers are immutable, `b` keeps old value)

**Q6 → C** — Output: `[10, 20]`
(list is mutable, `+=` modifies in place)

**Q7 → A** — Output: `10`
(integer passed by value-like behavior)

**Q8 → A** — Output: `10`
(function reads global `x`)

**Q9 → C** — Error
(UnboundLocalError: local `x` referenced before assignment)

**Q10 → B** — Output:

```
10
30
```

---

## 🔹 Lists, Dicts & Iteration (Q11–Q20)

**Q11 → B** — Output: `[3, 2, 1]`

**Q12 → B** — Output: `[2, 3]`

**Q13 → A** — Output: `[1, 10, 2, 3]`

**Q14 → A** — Output: `10`

**Q15 → A** — Output: `True`

**Q16 → C** — Output: `{'a': 1, 'b': 5}`

**Q17 → B** — Output: `[2, 4, 6]`

**Q18 → A** — Output: `[1, 4, 9]`

**Q19 → A** — Output:

```
a
b
```

(keys printed)

**Q20 → B** — Output: `[(0,1),(1,2),(2,3)]`

---

## 🔹 Loops & Control Flow (Q21–Q30)

**Q21 → A** — Output:

```
1
3
```

**Q22 → A** — Output:

```
0
1
2
```

**Q23 → A** — Output:

```
0
1
2
```

**Q24 → A** — Output:

```
0
1
3
4
```

**Q25 → B** — Output: `6 outputs`

**Q26 → B** — Output: `[1, 3]`
(removing during iteration skips elements)

**Q27 → A** — Output:

```
1
2
3
```

**Q28 → C** — Output: `"Hi"` printed **3 times**

**Q29 → B** — Output: `[0, 1, 2]`

**Q30 → A** — Output: `Done`

---

## 🔹 Functions, OOP & Decorators (Q31–Q40)

**Q31 → C** — Output: `15`

**Q32 → C** — Output: `3`

**Q33 → B** — Output: `{'a': 1, 'b': 2}`

**Q34 → B** — Output: `None`

**Q35 → B** — Output: `Hi`

**Q36 → A** — Output: `Hi`

**Q37 → C** — Output: `<class '__main__.A'>`

**Q38 → A** — Output: `A`

**Q39 → B** — Output: `10`
(name mangling: `_A__x`)

**Q40 → B** — Output:

```
Before
Hello
```

---

## 🎯 Summary

- ✅ All answers verified
- ✅ No ambiguous questions
- ✅ Excellent **conceptual depth for students**
- ✅ Very good for **tests + interviews**

---
