---
title: "JavaScript Assignment: Rest, Spread & Advanced Patterns"
description: "Practice tasks for rest parameters, spread operator, destructuring, and advanced array/object handling."
---

# JavaScript Assignment: Rest, Spread & Advanced Patterns

### 41. Sum all numbers with rest
```js
function sumAll(...numbers) {
  // Write logic here
}
```
Task: Use rest parameter to accept multiple numbers and return their sum.

### 42. Find maximum with rest
```js
function findMax(...numbers) {
  // Write logic here
}
```
Task: Return the largest number from all given arguments.

### 43. Merge arrays with spread
```js
function mergeArrays(arr1, arr2) {
  // Write logic here
}
```
Task: Merge both arrays using spread operator and return a new array.

### 44. Copy array with spread
```js
function copyArray(arr) {
  // Write logic here
}
```
Task: Return a new copy of the given array using spread operator.

### 45. Copy object with spread
```js
function copyObject(obj) {
  // Write logic here
}
```
Task: Return a copy of the given object using spread operator.

### 46. Merge objects with spread
```js
function mergeObjects(obj1, obj2) {
  // Write logic here
}
```
Task: Merge both objects into a new object using spread operator.

### 47. Rest parameter and destructuring
```js
function printStudent(first, ...subjects) {
  // Write logic here
}
```
Task: Print first argument as student name and remaining arguments as subjects.

### 48. Add new value using spread
```js
function addNewValue(arr, value) {
  // Write logic here
}
```
Task: Return a new array with `value` added at the end using spread operator.

### 49. Update object property using spread
```js
function updateUser(user, newCity) {
  // Write logic here
}
```
Task: Return updated object with new city using spread operator.

### 50. Combine strings with rest
```js
function joinWords(...words) {
  // Write logic here
}
```
Task: Join all words into a single sentence and return it.

### 51. Skip first value with destructuring + rest
```js
function getRemainingValues(arr) {
  // Write logic here
}
```
Task: Skip the first value and return remaining values using rest operator.

### 52. Extract first two values and rest
```js
function splitArray(arr) {
  // Write logic here
}
```
Task: Extract the first two values separately and return the remaining values in another array.

### 53. Call function with spread
```js
function multiply(a, b, c) {
  // Write logic here
}

const nums = [2, 3, 4];
```
Task: Call the function using spread operator and return the multiplication result.

### 54. Clone and update array
```js
function updateArray(arr, newValue) {
  // Write logic here
}
```
Task: Create a copy of the array and add `newValue` using spread operator.

### 55. Merge multiple arrays
```js
function combineAll(arr1, arr2, arr3) {
  // Write logic here
}
```
Task: Merge all three arrays using spread operator.

### 56. Count total arguments with rest
```js
function countArgs(...args) {
  // Write logic here
}
```
Task: Return the total number of arguments passed.

### 57. Remove first argument with rest
```js
function removeFirst(first, ...restValues) {
  // Write logic here
}
```
Task: Return the remaining values after removing the first argument.

### 58. Update nested object using spread
```js
function updateProfile(profile) {
  // Write logic here
}
```
Task: Update profile city to `"Lahore"` using spread operator.

### 59. Multiply all numbers with rest
```js
function multiplyAll(...numbers) {
  // Write logic here
}
```
Task: Multiply all numbers passed and return the result.

### 60. Convert arguments into array
```js
function makeArray(...values) {
  // Write logic here
}
```
Task: Accept multiple values using rest parameter and return them as an array.
