---
title: "JavaScript Assignment: Promises & Async"
description: "Practice tasks for Promise creation, chaining, async/await, and error handling."
---

# JavaScript Assignment: Promises & Async

### 121. Basic Promise creation

```js
function createPromise() {
  // Write logic here
}
```

Task: Create a Promise that resolves with `"Promise Resolved"`.

### 122. Promise with reject

```js
function checkNumber(num) {
  // Write logic here
}
```

Task: Resolve if number is greater than 10, otherwise reject.

### 123. Using `.then()` and `.catch()`

```js
function fetchData() {
  // Write logic here
}
```

Task: Create a Promise and handle success using `.then()` and error using `.catch()`.

### 124. Promise with setTimeout

```js
function delayedPromise() {
  // Write logic here
}
```

Task: Resolve `"Done after 2 seconds"` using `setTimeout` inside a Promise.

### 125. Async function example

```js
async function getMessage() {
  // Write logic here
}
```

Task: Return `"Hello from async function"`.

### 126. Async/Await with Promise

```js
function getUser() {
  // Write logic here
}

async function fetchUser() {
  // Write logic here
}
```

Task: Create a Promise in `getUser()` and use `await` inside `fetchUser()`.

### 127. Handle rejected Promise with try/catch

```js
function loginUser() {
  // Write logic here
}

async function handleLogin() {
  // Write logic here
}
```

Task: Reject the Promise from `loginUser()` and handle the error using try/catch.

### 128. `Promise.all()` example

```js
function promiseOne() {
  // Write logic here
}

function promiseTwo() {
  // Write logic here
}
```

Task: Use `Promise.all()` to resolve both promises together.

### 129. `Promise.race()` example

```js
function fastPromise() {
  // Write logic here
}

function slowPromise() {
  // Write logic here
}
```

Task: Use `Promise.race()` to return the first resolved promise.

### 130. Async function with delay

```js
async function waitAndPrint() {
  // Write logic here
}
```

Task: Wait for 3 seconds using a Promise and `await`, then print a message.

### 131. Convert callback to Promise

```js
function callbackExample(callback) {
  // Write logic here
}
```

Task: Convert callback-based logic into Promise-based logic.

### 132. Promise chaining

```js
function stepOne() {
  // Write logic here
}
```

Task: Create Promise chaining with `stepOne → stepTwo → stepThree`.

### 133. Fetch-like Promise simulation

```js
function fetchProducts() {
  // Write logic here
}
```

Task: Return a Promise with fake product data after 2 seconds.

### 134. Async function returning object

```js
async function getProfile() {
  // Write logic here
}
```

Task: Return an object with `name` and `role` using an async function.

### 135. Multiple `await` example

```js
function getName() {
  // Write logic here
}

function getRole() {
  // Write logic here
}

async function getDetails() {
  // Write logic here
}
```

Task: Use multiple `await` calls and print both values.

### 136. `Promise.finally()` example

```js
function paymentStatus() {
  // Write logic here
}
```

Task: Use `.finally()` after Promise completion.

### 137. Reject Promise manually

```js
function checkAge(age) {
  // Write logic here
}
```

Task: Reject the Promise if `age` is less than 18.

### 138. Nested async function

```js
async function outerFunction() {
  // Write logic here
}
```

Task: Create an inner async function and call it inside `outerFunction()`.

### 139. API simulation using Promise

```js
function getPosts() {
  // Write logic here
}
```

Task: Simulate an API response using a Promise and return a posts array.

### 140. Real-world login flow

```js
function verifyUser(email, password) {
  // Write logic here
}
```

Task: Resolve if email and password are correct, otherwise reject using a Promise.
