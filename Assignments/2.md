---
title: "JavaScript Assignment: Functions & Fundamentals"
description: "Complete these basic JavaScript function tasks using callbacks, destructuring, arrays, and object operations."
---

# JavaScript Assignment: Functions & Fundamentals

### 1. Add two numbers

```js
function add(a, b) {
  // Write logic here
}
```

Task: Complete the function to return the sum of `a` and `b`.

### 2. Check even or odd

```js
function checkEvenOdd(num) {
  // Write logic here
}
```

Task: Return `"Even"` if number is even, otherwise return `"Odd"`.

### 3. Find the largest of two numbers

```js
function largestOfTwo(a, b) {
  // Write logic here
}
```

Task: Return the larger number between `a` and `b`.

### 4. Reverse a string

```js
function reverseString(str) {
  // Write logic here
}
```

Task: Reverse the given string and return it.

### 5. Count vowels

```js
function countVowels(str) {
  // Write logic here
}
```

Task: Count total vowels (`a`, `e`, `i`, `o`, `u`) in the string.

### 6. Sum of array

```js
function sumArray(arr) {
  // Write logic here
}
```

Task: Return the sum of all numbers in the array.

### 7. Callback example

```js
function processUser(name, callback) {
  // Write logic here
}
```

Task: Pass the name to the callback function and execute it.

### 8. Delayed message

```js
function delayedMessage() {
  // Write logic here
}
```

Task: Print `"Welcome Student"` after 2 seconds.

### 9. Object destructuring

```js
const user = {
  name: "Ali",
  age: 22,
  city: "Karachi",
};

// Write destructuring here
```

Task: Extract `name` and `age` from the `user` object.

### 10. Array destructuring

```js
const numbers = [10, 20, 30, 40];

// Write destructuring here
```

Task: Extract first and second values into variables.

### 11. Destructured parameters

```js
function printUser({ name, email }) {
  // Write logic here
}
```

Task: Print `name` and `email` using destructured parameters.

### 12. Remove duplicates from array

```js
function removeDuplicates(arr) {
  // Write logic here
}
```

Task: Return a new array without duplicate values.

### 13. Double numbers with map()

```js
function doubleNumbers(arr) {
  // Write logic here
}
```

Task: Return a new array where each value is doubled.

### 14. Filter even numbers

```js
function getEvenNumbers(arr) {
  // Write logic here
}
```

Task: Return only even numbers from the array.

### 15. Total sum with reduce()

```js
function totalSum(arr) {
  // Write logic here
}
```

Task: Return the total sum using `reduce()`.

### 16. Check palindrome

```js
function isPalindrome(str) {
  // Write logic here
}
```

Task: Return `true` if string is palindrome, otherwise `false`.

### 17. Swap values

```js
function swapValues(a, b) {
  // Write logic here
}
```

Task: Swap values using destructuring and return both values.

### 18. Return another function

```js
function outerFunction() {
  // Write logic here
}
```

Task: Return another function that prints `"Inner Function Called"`.

### 19. Merge arrays

```js
function mergeArrays(arr1, arr2) {
  // Write logic here
}
```

Task: Merge both arrays using spread operator.

### 20. Copy object

```js
function copyObject(obj) {
  // Write logic here
}
```

Task: Return a copy of object using spread operator.
