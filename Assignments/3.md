---
title: "JavaScript Assignment: Intermediate Functions"
description: "Tasks for intermediate JavaScript patterns including loops, array methods, destructuring, and callbacks."
---

# JavaScript Assignment: Intermediate Functions

### 21. Calculate factorial
```js
function factorial(num) {
  // Write logic here
}
```
Task: Return the factorial of the given number.

### 22. Check prime number
```js
function isPrime(num) {
  // Write logic here
}
```
Task: Return `true` if number is prime, otherwise `false`.

### 23. Find largest number in array
```js
function largestInArray(arr) {
  // Write logic here
}
```
Task: Return the largest number from the array.

### 24. Find smallest number in array
```js
function smallestInArray(arr) {
  // Write logic here
}
```
Task: Return the smallest number from the array.

### 25. Capitalize first letter
```js
function capitalizeFirst(str) {
  // Write logic here
}
```
Task: Convert the first letter of the string to uppercase.

### 26. Count words in sentence
```js
function countWords(sentence) {
  // Write logic here
}
```
Task: Return total number of words in the sentence.

### 27. Use forEach()
```js
function printElements(arr) {
  // Write logic here
}
```
Task: Use `forEach()` to print all array elements.

### 28. Use find()
```js
function findGreaterThanTen(arr) {
  // Write logic here
}
```
Task: Return the first number greater than 10.

### 29. Use some()
```js
function hasNegativeNumber(arr) {
  // Write logic here
}
```
Task: Return `true` if the array contains any negative number.

### 30. Use every()
```js
function allPositive(arr) {
  // Write logic here
}
```
Task: Return `true` if all numbers are positive.

### 31. Sort array ascending
```js
function sortAscending(arr) {
  // Write logic here
}
```
Task: Return the array sorted in ascending order.

### 32. Sort array descending
```js
function sortDescending(arr) {
  // Write logic here
}
```
Task: Return the array sorted in descending order.

### 33. Callback function example
```js
function greetUser(name, callback) {
  // Write logic here
}
```
Task: Print a greeting and then execute the callback function.

### 34. Function with setInterval()
```js
function printNumbers() {
  // Write logic here
}
```
Task: Print numbers from 1 to 5 using `setInterval()`.

### 35. Nested object destructuring
```js
const student = {
  name: "Ahmed",
  marks: {
    math: 90,
    english: 85
  }
};

// Write destructuring here
```
Task: Extract `math` and `english` marks.

### 36. Default values in destructuring
```js
const employee = {
  name: "Sara"
};

// Write destructuring here
```
Task: Extract `name` and `role`, where `role` should default to `"Developer"`.

### 37. Rename variables in destructuring
```js
const product = {
  title: "Laptop",
  price: 50000
};

// Write destructuring here
```
Task: Extract `title` as `productName` and `price` as `productPrice`.

### 38. Convert array into string
```js
function arrayToString(arr) {
  // Write logic here
}
```
Task: Convert array values into a single string separated by commas.

### 39. Check if string contains a word
```js
function containsWord(sentence, word) {
  // Write logic here
}
```
Task: Return `true` if the word exists inside the sentence.

### 40. Higher-order function example
```js
function calculator(a, b, operation) {
  // Write logic here
}
```
Task: Accept `operation` as a callback and execute it on `a` and `b`.
